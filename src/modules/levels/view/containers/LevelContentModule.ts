import withModule from '@common/hocs/withModule';
import LevelContent from './LevelContent';

const LevelContentModule = withModule({
  key: 'LevelContentModule',
  component: LevelContent,
  providers: []
});

export default LevelContentModule;
